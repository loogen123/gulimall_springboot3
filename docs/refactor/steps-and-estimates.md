# 重构步骤清单（按模块优先级）

- 本文件为手工维护，用于列出模块级步骤与预计工时（人时）。

## P0

### 基座/门禁/配置/测试基建（预计 72h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

### gulimail-common（预计 24h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

### gulimail-third-party（预计 16h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

阻塞/前置：
- 测试/启动依赖缺失配置占位符，当前阻塞 root mvn test

### gulimail-seckill（预计 24h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

阻塞/前置：
- Sentinel WebMVC 适配缺失导致上下文无法启动

### gulimail-search（预计 24h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

阻塞/前置：
- 测试依赖 ES 运行态索引 bank 导致不稳定

### gulimail-order（预计 56h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

### gulimail-ware（预计 48h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

## P1

### gulimail-gateway（预计 24h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

### gulimail-cart（预计 24h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

### gulimail-product（预计 32h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

### gulimail-member（预计 24h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

## P2

### gulimail-coupon（预计 32h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

阻塞/前置：
- 是否纳入“优惠券核销闭环”需在阶段 0 明确范围

### gulimail-auth-server（预计 16h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点

### gulimail-ai（预计 16h）

1. 建立分层骨架（api/application/domain/infrastructure），梳理依赖边界
2. 统一异常与响应规范接入，去除 controller 局部兜底与返回不一致
3. 拆分关键用例：用例编排/领域规则/基础设施适配
4. 补齐单元测试与集成测试（优先覆盖主链路与失败分支）
5. 性能与回归验证，输出对比数据与风险回滚点
